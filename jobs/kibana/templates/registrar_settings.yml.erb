message_bus_servers:
<% p('nats.machines').each do |ip| %>
    - host: <%= ip %>:<%= p('nats.port') %>
      user: <%= p('nats.user') %>
      password: <%= p('nats.password') %>
<% end %>
external_host: <%= p('external_host') %>
external_ip: <%= spec.networks.default.ip %>
port: 5601
health_checker:
    name: script
    interval_in_seconds: 10
    healthcheck_script_path: /var/vcap/jobs/kibana/bin/healthcheck.sh
